<div class="container log">
    <div class="row">

    <div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">

        {{#if session.isAuthenticated}}

        	<button type="button" class="btn btn-info" {{action "logout"}}>Odjava</button>

        {{else}}
        <div class="form center-block">
        <i class="fa fa-user" aria-hidden="true"></i>
                <div class="login-input">
                    {{input placeholder="Korisničko ime" value=model.korisnicko_ime}}
                    <br><br>
                    {{input type="password" placeholder="Šifra" id='pwInput' value=model.sifra}}
                </div>

                <br><br>

                <div class="login-input">
                    <button type="button" id="login-btn" class="btn btn-info" disabled={{ isToSAccepted }} {{action "login"}}>PRIJAVA</button>
                    <h4 style="color:red">{{errorMessage}}</h4>
                </div>
        </div>
        {{/if}} 

       </div> 
    </div>
</div>
<script>
$(document).ready(function () {
    $('#pwInput').on('keyup', function (e) {
       if(e.which == 13) 
        $('#login-btn').click();
    });
});
</script>